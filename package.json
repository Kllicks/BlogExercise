{
  "name": "BlogExercise",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "debug": "node --inspect-brk index.js",
    "dev": "nodemon index.js",
    "reset-db": "dropdb blog-db; createdb blog-db; psql -f schema.sql blog-db; psql -f seed.sql blog-db",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.6"
  },
  "dependencies": {
    "pg-promise": "^8.5.1"
  }
}
